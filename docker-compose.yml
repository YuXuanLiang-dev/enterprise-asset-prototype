version: '3.8'

services:
  asset-mysql:
    image: mysql:8.0
    container_name: asset-mysql
    restart: always
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      TZ: Asia/Shanghai
    command: 
      --character-set-server=utf8mb4
      --collation-server=utf8mb4_unicode_ci
      --default-authentication-plugin=mysql_native_password
    volumes:
      - ./mysql_data:/var/lib/mysql  # 如有遗留的 1/mysqldata 旧目录可删除后再启动
